---
description: <Stack> development standards for Rolos projects
globs: <glob patterns for this stack, e.g. **/*.py or src/**/*.ts>
alwaysApply: false
---

# <Stack> Standards

Follow the canonical standard: **`.ai/standards/stacks/<stack-id>/development.md`**

Key rules:
- <top 3-4 stack-specific rules>

Agent: `.ai/agents/stacks/<stack-id>/backend-developer.yaml`
Skills: <stack-specific review/scaffolding skills, if any>

<!--
To enable: copy this file to .cursor/rules/<stack-id>.mdc (drop the .example
suffix), set `globs` to the file patterns for the stack, and fill in the brackets.
See docs/ADDING-A-LANGUAGE.md for the full process.
-->
